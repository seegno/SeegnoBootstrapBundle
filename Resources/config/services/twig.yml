parameters:
    seegno_bootstrap.twig_extension.class: Seegno\BootstrapBundle\Twig\Extension\SeegnoBootstrapExtension

services:
    seegno_bootstrap.twig_extension:
        class: %seegno_bootstrap.twig_extension.class%
        arguments: [@session, %seegno_boostrap.alerts%]
        tags:
            - { name: twig.extension }
